---
-
  manager: map
  interface:
    -
      operation: reserve
      argument:
        - (capability)
        - t_opts
        - t_vsize
        - t_perms
        - (out) t_vaddr*
    -
      operation: release
      argument:
        - (capability)
        - t_vaddr
    -
      operation: get
      argument:
        - (capability)
        - (out) o_map**


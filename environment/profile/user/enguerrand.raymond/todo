# Rewrite the MBL script with new variables
# Redefinition of variable names in the platform mips64.conf
# Rewrite the libc.h macro functions
# Check inclusion system in firmware and bootloader
# Write MIPS64 book about firmware, memory mapping and bootloader
# Rewrite multiboot becaus type modification for 64 bits are note legal

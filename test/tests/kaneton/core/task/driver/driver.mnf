name: core/task/driver
description: This test creates a driver-class task with which a segment is shared with the kernel address space. In addition a thread is spawn within the kernel which will stop the scheduler after some time. The created task's thread writes data to the shared segment while the main thread verifies that the proper value has been written.
symbol: test_core_task_driver
signature: vnweoiwf30f9i3gi390dfi23jwmelcewgkweg
output:
points: 3
timeout:
  qemu: 12
  xen: 9

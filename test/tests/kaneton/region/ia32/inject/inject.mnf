name: region/ia32/inject
description: This test creates a task and address space, then builds a region by allocating memory directly through malloc(). Finally, the memory region is mapped and injected.
symbol: test_region_ia32_inject
output:
